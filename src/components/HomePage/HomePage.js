import React from "react";

import style from "./HomePage.module.css";

const HomePage = () => {
  return (
    <section className={style.container}>
      <h2 className={style.message}>HomePage</h2>
    </section>
  );
};

export default HomePage;